Archived files live here to keep the active repo focused and cohesive.

- Anything under `.archive/` is **not** maintained and may be out of date.
- The unified API + UI entrypoint is `./start.sh` (FastAPI + React dashboard).

